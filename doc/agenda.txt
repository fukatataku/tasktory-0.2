[Open]
02. チェックアウトした時に既存のメモを残すかどうか

04. フィルターコンフィグの書き方

05. ロガーの仕様
    - ファイルに書き込む部分はsingletonにする
    - クラス名とメソッド名を自動で取るようにする
    - YYYY-mm-ddTHH:MM:SS INFO Tasktory.restore START args=(...)&kwargs={...}

[Closed]
01. ROOTがタスクトリになっていないので、チェックアウトしても何も出ない
    [解決案]
    - （採用）ジャーナルから読み込む際に'/'もパスとして取り出してタスクトリにする
    - ジャーナルとは別に初期化処理を入れて、そこでROOTをタスクトリにする

03. タスクトリにタグを付加する方法
    [却下]
    レポート向けのフィルタではタグ解析は不要。単にパスに特定の名前が含まれてい
    るかどうかで対応できる。

